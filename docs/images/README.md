# README screenshots

Drop these PNGs here (referenced by the top-level README.md):

- `setup-ui.png`         — the Setup/launcher page at http://localhost:8730/
- `diagnostics.png`      — a gate's /radio diagnostics page (its --ctl-port)
- `ae-band-narrowing.png`— an IC-9700 in AetherSDR showing only 2m/440/23cm
                           (the radio-declared-bands result — the hero image)
- `ae-chooser.png`       — (optional) AE's radio chooser with the gate(s) listed
